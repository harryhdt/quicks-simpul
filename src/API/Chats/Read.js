import { base_url, headers } from "../../const/api";

const getAllChatLists = async () => {
  try {
    // code
  } catch ($exception) {
    console.error($exception);
    return [];
  }
};

const getChat = async (id) => {
  try {
    // code
  } catch ($exception) {
    console.error($exception);
    return null;
  }
};

export { getAllChatLists, getChat };
